# Both synthetic ground-truth coefficient maps

Exact analytic additive and pairwise coefficient matrices for instance 00 of
the structured positive-control and unstructured negative-control synthetic
ground truths. These are loaded directly from each `gt_params.npz`; they are
not surrogate fits or saturation-derived estimates.

```bash
python mRNA_RBP/scripts/figures/prototypes/synthetic_gt_coefficient_maps/make_figure.py
```
