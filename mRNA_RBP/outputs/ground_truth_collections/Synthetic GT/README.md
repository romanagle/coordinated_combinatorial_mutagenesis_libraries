# Synthetic GT

Figures are in `figures/`; copied cache/model/weight artifacts are in `cached_libraries/`. Synthetic source libraries remain in `mRNA_RBP/outputs/instance_*` and are copied into this collection as cached artifacts. The manifest records every file included in the curated collection.

- Figures copied in manifest: `12`
- Cached artifacts copied in manifest: `680`
- Missing source files: `0`
