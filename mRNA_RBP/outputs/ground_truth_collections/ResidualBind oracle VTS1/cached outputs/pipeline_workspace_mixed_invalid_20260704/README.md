# ResidualBind VTS1 pipeline workspace

Active outputs only:

- `libraries_used_for_figures/`: clean current libraries used for the figures.
- `lib_size_spearman_results.json`: completed ResidualBind VTS1 high-WT pipeline results.

No `type2.npz` files are used here. The activity-balanced library is `libraries_used_for_figures/activity_balanced.npz`.

Current-run coefficient arrays were archived under `../cached outputs/current_run_surrogate_coefs_20260704/` because coefficient-map figures are not part of the active figure set.
