# Canonical outputs

For manuscript-facing evidence, go directly to
[`ground_truth_collections/`](ground_truth_collections/).

The other files and directories here are legacy synthetic-GT working artifacts.
They are not an additional curated result collection and should not be used as
the navigation source for the manuscript.

Generated oracle workspaces now live under [`../runs/`](../runs/), while fitted
runtime models live under [`../models/`](../models/). See
[`../docs/OUTPUT_DIRECTORIES.md`](../docs/OUTPUT_DIRECTORIES.md) for the directory contract.
