## Daily Summary

### Projects
- [[Chemical probing G6]]

### Progress
- Reorganized `G6_chemical_probing` so each benchmark lives under its own `datasets/<Name>/` directory instead of loose files at the repo root.
- Moved the RMDB histogram/pipeline-note outputs, the four `prepare_*.py` converters, and the ShapeKnots/RNAfold benchmark tooling into their respective dataset directories, fixing every downstream path reference.
- Synced `G6_chemical_probing` to its existing GitHub remote so the cluster can `git pull` the same layout instead of relying on a transfer tarball.

### Problems solved
- Cleaned stray duplicate output files out of `datasets/IRES/` (11 files/dirs that exactly duplicated `results/DNA_IRES/`; archived the one differing `.bak` manifest instead of deleting it).
- Found `G6_chemical_probing/` was already an independently git-tracked repo with a real `origin`, carrying a large pre-existing uncommitted diff (`manifest`→`input_sequences_table` rename, vendored `g6_fold_cyk.py`) that predated this session; reviewed and pushed it alongside today's changes instead of losing track of it.
- Resolved a `git add` "embedded repository" warning for `datasets/viennaRNASHAPE/` by dropping its nested `.git` (it pointed at `stanti/shapebenchmark`, a fork the user doesn't control) and tracking the content directly.

### Open problems
- The Deigan_2009 16S/23S cluster run hasn't actually been submitted yet — interactive Cannon SSH needs Duo/password auth this session can't complete.
- `G6_chemical_probing`'s cluster-side `rscape_v2.6.4` and `Rfam.cm` paths are assumed to still be present from the prior IRES transfer; not independently verified this session.

### Decisions
- Track `datasets/viennaRNASHAPE/` directly in `G6_chemical_probing` rather than as a nested git clone, since its origin (`stanti/shapebenchmark`) is a fork the user doesn't control.
- Use `G6_chemical_probing`'s existing GitHub remote (`romanagle/G6_chemical_probing`) to sync repo organization for the cluster run, rather than building a one-off transfer tarball like the prior Human RNAmap IRES cluster run.
- Keep the root `rivaslab2026` repo separate from and excluding `G6_chemical_probing/` and `humanrnamap/`, matching the existing "vendored, kept standalone" precedent from `humanrnamap/NOTES.md`.

### Key results
- `Deigan_2009`'s 5-record input table has only 3 completed folds; `deigan_16s` and `deigan_23s` were never prepared or folded, which is why they need the cluster.

| Deigan_2009 record | Length (nt) | Status |
| --- | ---: | --- |
| deigan_trna | 75 | complete |
| deigan_hcv_domain2 | 95 | complete |
| deigan_p546 | 155 | complete |
| deigan_16s | 1,542 | pending (cluster) |
| deigan_23s | 2,904 | pending (cluster) |

### Next steps

**[[Chemical probing G6]]**

[[Tasks/Chemical probing G6|Canonical task list]]

- [ ] On the cluster, `git pull` inside `G6_chemical_probing` and submit `slurm/run_reactivity_g6.sbatch` against `datasets/Deigan_2009/input_sequences_table.tsv` to fold `deigan_16s`/`deigan_23s`.
- [ ] Compare RNAfold, ShapeKnots, and G6 SHAPE effects on the 24-RNA benchmark.
- [ ] Retrieve and validate the completed Human RNAmap IRES outputs.
- [ ] Clean up the `G6_chemical_probing` codebase for a public GitHub release to send to PI.

**[[random_lib_mutagenesis]]**

[[Tasks/random_lib_mutagenesis|Canonical task list]]

- [ ] Update the Part III, Section 2 interpretation for the accepted residual violin figure, focusing on overprediction and underprediction before versus after four mutations (approximately the 10% mutation rate).
- [ ] Refine the accepted Part I model-misspecification subpoint and decide which comparisons to retain in the two-panel Spearman/RMSE figure.
- [ ] Create a Methods figure showing the workflow from ResidualBind → deepSQUID → surrogate-model training → evaluation libraries.
- [ ] Rerun full pipeline across 10 initializations; regenerate Synthetic GT, VTS1, and HuR high-WT library-size figures.
- [ ] Build an activity × motif-status × mutation-count occupancy map for VTS1 and identify sparse mechanistic cells.

### Starting Context
- ShapeKnots and RNAfold SHAPE benchmarks are both complete (24/24 RNAs each); the three-way RNAfold/ShapeKnots/G6 comparison is still outstanding.
- `G6_chemical_probing` now mirrors this organization on `origin/main`; no cluster job has been submitted yet.
- YbeY and random_lib_mutagenesis were not worked on today; see their canonical task lists for the current queue.

### Handoff
- `G6_chemical_probing`'s dataset layout and today's reorg are pushed to `origin/main` (commits `4c559c5`, `8d3e70c`); a plain `git pull` on the cluster gets the same paths used in `slurm/run_reactivity_g6.sbatch`.
- Next concrete action is the actual `sbatch` submission for `deigan_16s`/`deigan_23s` — needs to happen interactively on Cannon.

---

## Session History

### 13:02 — Claude

**Project:** [[Chemical probing G6]]

- Objective: Reorganize `G6_chemical_probing`'s per-dataset outputs into `datasets/<Name>/` directories and sync the repo to git so the cluster sees the same layout for a Deigan 16S/23S cluster run.
- Work completed: Moved RMDB histograms/pipeline note, the four `prepare_*.py` scripts, and the ShapeKnots/RNAfold benchmark tooling (`data/shapebenchmark/` → `G6_chemical_probing/datasets/viennaRNASHAPE/`, including folding in its nested git history) into their respective dataset directories; fixed every downstream path reference (README, `prepare_deigan2009.py`'s cross-import, `shapebenchmark_rfam_cacofold.py` defaults). Cleaned stray duplicate output files out of `datasets/IRES/`. Initialized a root `rivaslab2026` git repo (scoped `.gitignore` excluding bulk caches/vendored binaries/nested repos) and committed and pushed `G6_chemical_probing`'s pending changes to its existing GitHub remote.
- Problems solved: Found `G6_chemical_probing/` was already an independently git-tracked repo with a real `origin`, sitting on a large pre-existing uncommitted diff that predates this session; verified and pushed it alongside today's changes rather than losing track of it.
- New results: Discovered Deigan_2009's input table has 5 records but only 3 (all ≤155 nt) have ever been prepared/folded; `deigan_16s` (1,542 nt) and `deigan_23s` (2,904 nt) were never run, which is why they need the cluster.
- Remaining issue: The actual `sbatch` submission for `deigan_16s`/`deigan_23s` hasn't run yet — interactive cluster SSH requires Duo/password auth this session can't complete.
- Immediate next step: On the cluster, `git pull` inside `G6_chemical_probing` and submit `slurm/run_reactivity_g6.sbatch` against `datasets/Deigan_2009/input_sequences_table.tsv`.

---
